// Note(sagar): node modules should be enabled for this
import assert from "assert";
import { resolve } from "builtin://path";

const resolved = resolve("./simple.js");
const run = async () => {};

run();
