// Note(sagar): adding this for VScode auto-complete
// even though tailwind isn't used
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,css,js,jsx,ts,tsx}"],
};
