{
  "name": "@arena/workspace-server",
  "version": "0.1.0",
  "description": "A workspace server for Arena runtime",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "pnpm esbuild index=./src/index.ts --outdir=dist --format=esm --bundle --minify",
    "dev": "pnpm esbuild index=./src/index.ts --outdir=dist --format=esm --bundle --watch"
  },
  "author": "Sagar Poudel",
  "private": true,
  "license": "UNLICENSED",
  "dependencies": {
    "@arena/core": "workspace:^0.1.0",
    "@arena/runtime": "workspace:^0.1.0",
    "path": "^0.12.7",
    "query-string": "^8.1.0",
    "util": "^0.12.5"
  },
  "devDependencies": {
    "esbuild": "^0.17.10"
  }
}
